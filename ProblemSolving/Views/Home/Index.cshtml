@model IEnumerable<ProblemSolving.Models.cat>

@{
    ViewBag.Title = "Home Page";
}


@foreach (var item in Model)
{
    <div class="container-fluid padding">
        <div class="row welcome text-center">
            <div class="col-12">
                <h1 class="display-4">@Html.DisplayFor(modelItem => item.CategoryName)</h1>
                <br><hr><br>
            </div>
        </div>
        <h3>@Html.DisplayFor(modelItem => item.CategoryDescription)</h3>
    </div>


    <div class="container padding">
        <div class="row padding">

            @foreach (var Subitem in item.problem)
            {
               string img = Subitem.probImg;
                <div class="col-md-4">
                    <div class="card" style="width: 14rem;">
                         <img  class="card-img-top" src="@Url.Content("~/Img/aa.png")" style="width:102%" />

                        <div class="card-body">
                            <h4 class="card-title">
                                @Html.DisplayFor(ModelItem => Subitem.ProblemName)
                            </h4>
                            <p class="card-text">@Html.DisplayFor(ModelItem => Subitem.ProblemDescription)</p>

                            @Html.ActionLink("SubItemsV", "SubItemsV", new { ProblemID = Subitem.ProblemID }, new { @class = "btn btn-outline-secondary" })

                        </div>
                    </div>
                </div>

            }
        </div>
    </div>
}

<hr class="my-4">         <!--Two Column Section-->
<div class="container-fluid padding">

    <div class="row padding">

        <div class="col-lg-6">

            <h2>Our Aim is To Help People</h2>
            <p>
                Is the aim is to help us,Is the aim is to help us,Is the aim is to help us,Is the aim is to help us,
                Is the aim is to help us,Is the aim is to help us,Is the aim is to help us
            </p>
            <br>
        </div>
        <div class="col-lg-6">
            <img src="images/desk.jpg" class="img-fluid">
        </div>
    </div>
</div>
